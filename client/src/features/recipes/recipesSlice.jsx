import { createSlice } from "@reduxjs/toolkit";
const initialState = {

};

const recipesSlice = createSlice({
  name: "recipe",
  initialState,
  reducers: {
    add: (store, action) => {

    },

    delete: (store, action) => {

    },

    // more
  }
})